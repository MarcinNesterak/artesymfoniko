# MongoDB
MONGODB_URI=mongodb://localhost:27017/orchestra

# JWT
JWT_SECRET=twoj_super_tajny_klucz

# Redis (do rate limiting)
REDIS_URL=redis://localhost:6379

# SMTP (do wysyłki emaili)
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=twoj_email@example.com
SMTP_PASS=twoje_haslo
SMTP_FROM=twoj_email@example.com

# Frontend URL (do linków weryfikacyjnych i resetu hasła)
FRONTEND_URL=http://localhost:3000